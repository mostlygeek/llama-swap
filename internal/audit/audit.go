// Package audit holds repository-level invariant checks that run as ordinary
// Go tests. Currently it keeps the gosec suppression ledger
// (docs/gosec-suppressions.md) in sync with the // #nosec markers in the code.
package audit
