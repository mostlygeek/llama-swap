# Copy to:
#   /etc/swap-laboratories/net-tune.env
#
# Example:
#   sudo install -m 0644 scripts/systemd/net-tune.env.sample /etc/swap-laboratories/net-tune.env

# Optional. If empty, only sysctl canary is applied.
IFACE=enp1s0f1np1

# perf|debug
MODE=perf
