.aider*
.env
build/
dist/
.vscode
.DS_Store
.dev/

# NOTE: internal/server/ui_dist/ is NOT ignored in this fork — the vanilla-JS UI
# is hand-authored and committed (embedded via //go:embed, no build step).

# Docs Agent eval results; regenerated by evals/docs-agent/run.sh
evals/docs-agent/runs/
evals/config.yaml
